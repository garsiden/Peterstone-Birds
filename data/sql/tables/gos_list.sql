DROP TABLE IF EXISTS "gos_list";
CREATE TABLE gos_list
(
	gos_code CHAR(4) NOT NULL CONSTRAINT GOS_CODE_PK PRIMARY KEY,
	species_name VARCHAR(40) NOT NULL CONSTRAINT GOS_LIST_SPECIES_NAME_UNIQ UNIQUE,
    scientific_name VARCHAR(40) NOT NULL CONSTRAINT GOS_LIST_SCIENTIFIC_NAME_UNIQ UNIQUE
);
