-# index.hml - ListController default view
%report
  %table.listing{:summary => @title}
    %thead
      %tr
        %th Code
        %th Species Name
        %th Scientific Name
      %tbody
      - @species_list.each do |k, v|
        %tr{:class => cycle}
          %td= k
          %td= v 
