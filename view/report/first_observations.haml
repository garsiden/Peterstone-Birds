=breadcrumb(request.request_uri)
%br
%br
%table.listing{:summary => @title}
  %caption= @caption
  %thead
    %tr
      - @headings.each do |h|
        %th= h
  %tbody
    - @array.each do |row|
      %tr{:class => cycle}
        %td.left= row[:species]
        - @years.each do |year|
          %td= fmt_date row[year], '%d %b'
        %td.total= fmt_date row[:earliest], '%d %b'
        %td.total= fmt_date row[:latest], '%d %b'
%br
%p.reportnote= last_updated 

